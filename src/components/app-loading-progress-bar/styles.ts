import {StyleSheet} from 'react-native';

export const appLoadingProgressBarstyles = StyleSheet.create({
  container: {
    width: '100%',
    justifyContent: 'center',
    alignItems: 'center',
  },
});
