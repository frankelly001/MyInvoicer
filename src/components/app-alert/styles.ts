import {StyleSheet} from 'react-native';

export const appAlertStyles = StyleSheet.create({
  container: {
    alignItems: 'center',
    justifyContent: 'center',
    marginTop: 100,
    paddingHorizontal: 60,
  },
  tttle: {marginTop: 16},
  desc: {marginTop: 8},
  btn: {width: undefined, marginTop: 32},
});
