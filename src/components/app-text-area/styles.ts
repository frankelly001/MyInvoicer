import {StyleSheet} from 'react-native';

export const appTextAreaStyles = StyleSheet.create({
  input: {
    justifyContent: 'flex-start',
    textAlignVertical: 'top',
  },
  container: {height: 90, paddingVertical: 5},
});
